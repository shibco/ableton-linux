# Test catalogue

Every test in the suite, what it covers, and where the behaviour came from.

**Generated by `tests/catalogue.sh` — do not edit by hand.** Test names come
from the files, and the *Guards* column from `# guards:` annotations above a
test. Run `./tests/catalogue.sh` after adding or renaming a test;
`tests/repo-hygiene.bats` fails when this file is stale.

117 tests across 7 suites. See [README.md](README.md) for how to run
them and [../.github/workflows/ci-checks.yml](../.github/workflows/ci-checks.yml)
for which run on a PR.

## Contents

- [tests/repo-hygiene.bats](#repo-hygiene) — 17 test(s)
- [tests/packaging.bats](#packaging) — 7 test(s)
- [tests/launcher-cli.bats](#launcher-cli) — 19 test(s)
- [tests/unit/detect-scale.bats](#detect-scale) — 20 test(s)
- [tests/unit/detect-theme.bats](#detect-theme) — 22 test(s)
- [tests/unit/launcher.bats](#launcher) — 20 test(s)
- [tests/patch-stack.bats](#patch-stack) — 12 test(s)

<a id="repo-hygiene"></a>

## tests/repo-hygiene.bats


Repo-wide invariants that cost seconds and have all been broken at least once.

Nothing here needs a build, a prefix, Wine, or a display. This is the tier
that should run on *every* push, unfiltered by paths.

| # | Test | Guards |
| --- | --- | --- |
| 1 | shellcheck: no warnings or errors in any shipped shell script | — |
| 2 | every shell script parses under its effective interpreter | — |
| 3 | the installer header survives being run by a real POSIX sh | scripts/setup-run-header.sh line 19 — the bash re-exec must precede every bash-ism |
| 4 | the installer header rejects unknown options instead of proceeding | — |
| 5 | file modes: launchers are executable, sourced and templated files are not | — |
| 6 | runtime name: every live file agrees on one wine-d2d1-nspa version | commit f84eaa4 — the 11.11 to 11.13 rename needed a follow-up pass |
| 7 | the wine base container-build.sh unpacks is the one vendor/ pins | — |
| 8 | VERSION is a well-formed dated release string | — |
| 9 | the test catalogue is current | — |
| 10 | vendored inputs match their pinned checksums | — |
| 11 | desktop entries validate after substitution | issue #106 — stuck on registering MIME types |
| 12 | desktop templates leave no unsubstituted @PLACEHOLDER@ behind | — |
| 13 | MIME package XML and icon SVGs are well-formed | — |
| 14 | every icon an installed desktop entry names is actually shipped | — |
| 15 | CI runs the bats tests/run.sh pins, not one of its own | a bats on PATH used to beat the pin, so a checkout ran whatever the |
| 16 | the vendored bats clone is ignored | .bats-core is a full clone of another project; run.sh's comment said it |
| 17 | build.sh forwards every variable container-build.sh reads from its environment | the container sees only what build.sh passes with -e, and an unset |

<a id="packaging"></a>

## tests/packaging.bats


Kit completeness: does the single-file installer actually carry everything the
scripts inside it reach for at runtime?

This is the blind spot behind the `installer`-labelled issues. A developer
runs scripts/ from a full checkout, where every path resolves. Users run the
staged kit, where only what make-installer.sh copied exists. Add a
`. "$here/detect-something.sh"` to setup-prefix.sh, forget the matching line
in make-installer.sh, and it works perfectly for the person who wrote it and
fails on every machine that installs from a release.

Nothing here builds or runs the installer — it reads make-installer.sh's
staging list and checks it against what the kit's own scripts reference.

| # | Test | Guards |
| --- | --- | --- |
| 1 | the kit staging list is still parseable out of make-installer.sh | the staging list is recovered by anchored sed, so a reformat of |
| 2 | every file make-installer.sh stages into the kit exists in the repo | — |
| 3 | every script a kit script sources is itself staged into the kit | issue label 'installer' — scripts resolve every path in a checkout, only some in the kit |
| 4 | every sibling file a kit script executes or installs is staged too | — |
| 5 | the kit ships the GPL source and licence Ableton Link requires | licence GPLv2+ — Ableton Link has no linking exception, so the source must travel with the binary |
| 6 | release.yml's asset list matches what make-installer.sh actually produces | — |
| 7 | every shell function a script calls is actually defined | lifting runtime_pids into the lib renamed it, and a replace that only |

<a id="launcher-cli"></a>

## tests/launcher-cli.bats


scripts/ableton-live — the launch contract, end to end.

The launcher runs for real here: discovery, the single-instance lock, registry
sync, argument routing, right up to the exec. What it would have exec'd is
captured instead of run, because ABLETON_WINE_ROOT points at a fake runtime
tree whose `wine` logs its argv and exits (see helpers/launcher.bash).

This is the half that users actually experience — which Live starts, what
happens with two installed, what a double-clicked .als does — and none of it
was covered by anything before.

| # | Test | Guards |
| --- | --- | --- |
| 1 | no Live installed: exits 1 and says how to install one | — |
| 2 | one install: launches it as a C: path with backslashes | — |
| 3 | two majors: the newest wins, and the choice is announced | — |
| 4 | two editions of one major: refuses to guess and lists them | — |
| 5 | ABLETON_LIVE_VERSION pins a major | — |
| 6 | ABLETON_LIVE_VERSION with no matching install names the majors present | — |
| 7 | ABLETON_LIVE_VERSION must be a number | — |
| 8 | ABLETON_LIVE_EXE pins an exact install and silences the ambiguity refusal | — |
| 9 | a .als set goes straight to the Live exe, never through start.exe | issue #38 — start.exe reads a bare unix path as a switch |
| 10 | clips and packs route the same way as sets, case-insensitively | issue #38 — .alc and .alp share the .als routing |
| 11 | a non-Live file keeps the start.exe /unix route | — |
| 12 | an ableton:// URL goes to start.exe verbatim | — |
| 13 | ABLETON_VDESK wraps the launch in a virtual desktop | — |
| 14 | a URL with no Live installed is still handed to start.exe | — |
| 15 | a second launcher refuses while the first is bringing Live up | — |
| 16 | realtime scheduling wraps the launch in chrt when rtprio is available | — |
| 17 | ABLETON_RT=off launches without chrt even when it would succeed | — |
| 18 | a stale wineserver is killed and the session booted before registry writes | scripts/ableton-live — reg.exe skips the wineboot wait; the FontSubstitutes HKLM write was lost this way |
| 19 | the launcher never leaves the fake runtime for the host's wine | — |

<a id="detect-scale"></a>

## tests/unit/detect-scale.bats


scripts/detect-scale.sh — display-scale detection and the scale -> DPI block map.

Two halves, tested differently:
  * the probes parse compositor output, so they get recorded fixtures and a
    stubbed binary. Whatever compositor the developer or CI runner is on has
    no bearing on the result.
  * the block map is pure arithmetic, so it gets an exhaustive table. That
    table is the calibration contract: changing a cell should be a deliberate
    act with a test diff to justify it, not a silent consequence.

| # | Test | Guards |
| --- | --- | --- |
| 1 | block map: gnome scales collapse onto the ceil-based matched set | scripts/detect-scale.sh DPI policy — mutter hands XWayland an integer-upscaled framebuffer |
| 2 | block map: non-gnome scales round to plain LogPixels with no IFEO | scripts/detect-scale.sh DPI policy — every other compositor expects application-side scaling |
| 3 | block map: scales outside 100-250% are refused, not clamped | — |
| 4 | block map: non-numeric scales are refused | — |
| 5 | block values: every block the map can emit decodes to LogPixels + IFEO | — |
| 6 | block values: LogPixels outside the validated 72..384 window is refused | — |
| 7 | block map round-trips: for_scale output is always a valid block | — |
| 8 | cosmic probe: a disabled lid never wins, even with no primary line | commit f0fc05e — cosmic-randr reports a Scale for a disabled output; older COSMIC emits no primary line |
| 9 | cosmic probe: a disabled lid never wins when it is marked non-primary | commit f0fc05e — the fallback loop did not check enabled state at all |
| 10 | cosmic probe: picks the Xwayland primary out of three enabled monitors | — |
| 11 | cosmic probe: declines when the desktop is not COSMIC | — |
| 12 | kde probe: Plasma 6 'priority 1' selects the primary output | — |
| 13 | kde probe: Plasma 5 'primary' keyword selects the primary output | — |
| 14 | gnome probe: reads the primary logical monitor, not the first | — |
| 15 | gnome probe: warns on stderr when monitors run mixed scales | — |
| 16 | xft probe: Xft.dpi becomes a scale | — |
| 17 | detect_scale: returns non-zero when no probe answers | — |
| 18 | detect_scale_ex: names the probe family that answered | — |
| 19 | detect_scale_ex: gnome outranks a lower-priority probe | — |
| 20 | detect_scale: normalises trailing zeros | — |

<a id="detect-theme"></a>

## tests/unit/detect-theme.bats


scripts/detect-theme.sh — host light/dark detection, titlebar colours, and
resolving which Ableton .ask theme Live is actually rendering with.

Everything here feeds the win32 [Control Panel\Colors] registry values the
launcher writes. A wrong value is not a crash: it is Live shipping with
unreadable chrome, which is exactly the kind of thing that reaches users.

| # | Test | Guards |
| --- | --- | --- |
| 1 | portal probe: color-scheme 1 is dark, 2 and 0 are light | — |
| 2 | gsettings probe: prefer-dark is dark, default and prefer-light are light | — |
| 3 | gsettings probe: an unrecognised scheme declines rather than guessing | — |
| 4 | detect_theme: the portal outranks gsettings | — |
| 5 | detect_theme: returns non-zero when no probe answers | — |
| 6 | rgb parse: accepts three 0-255 components | — |
| 7 | rgb parse: rejects out-of-range, short, long and non-numeric input | — |
| 8 | kde colours: prefers the Plasma 5.25+ Header set over legacy [WM] | — |
| 9 | kde colours: falls back to [WM] when Header is absent | — |
| 10 | kde colours: a malformed Header set falls through to [WM], not garbage | — |
| 11 | kde colours: declines with no kdeglobals at all | — |
| 12 | topbar colours: non-KDE hosts get the per-scheme constants | — |
| 13 | ask colour: reads 6-digit and 8-digit (alpha) hex, ignoring the alpha byte | — |
| 14 | ask colour: a malformed value or missing key fails, never prints a partial triple | — |
| 15 | newest prefs dir: mtime wins, not a version sort | scripts/detect-theme.sh — sort -V ranks 'Live 12.4/' after 'Live 12.4.3/' ('/' 0x2F > '.' 0x2E) |
| 16 | newest prefs dir: the sort -V trap case, stated explicitly | scripts/detect-theme.sh — same trap, asserted directly so the reason outlives the comment |
| 17 | newest prefs dir: a directory without Preferences.cfg is skipped | — |
| 18 | newest prefs dir: the major-version filter excludes other Live majors | — |
| 19 | newest prefs dir: prints nothing for a prefix with no Live at all | — |
| 20 | live theme file: falls back to the follow-system default per scheme | — |
| 21 | live theme file: fails when the fallback .ask is not installed either | — |
| 22 | live theme file: a theme named in Preferences.cfg beats the default | — |

<a id="launcher"></a>

## tests/unit/launcher.bats


scripts/ableton-live — the pure functions.

765 lines, 17 functions, and every one of them writes something into a Wine
prefix: registry colour triples, LOGFONT blobs, a CPU topology string. None of
it crashes when it goes wrong. It just renders Live slightly wrong, on someone
else's machine, in a way that arrives as "the menus look odd".

These are extracted and evaluated directly (see helpers/launcher.bash) because
the launcher runs discovery and exec at top level and cannot be sourced.
End-to-end launch behaviour is in tests/launcher-cli.bats.

| # | Test | Guards |
| --- | --- | --- |
| 1 | gray text: the dark fallback lands on classic GrayText | commit 9cba3b0 — the 45% GrayText blend, deduped between sync_win32_colors and theme_watch_loop |
| 2 | gray text: the light fallback stays mid-grey and readable | — |
| 3 | gray text: identical endpoints blend to themselves | — |
| 4 | gray text: the blend is 45% towards MenuText, per channel, not symmetric | issue #32 — menu bar chrome contrast |
| 5 | topbar override: a valid hex pair becomes two RGB triples | — |
| 6 | topbar override: uppercase hex is accepted | — |
| 7 | topbar override: anything but exactly two 6-digit hex colours is refused | — |
| 8 | logfont: a name packs to exactly 64 bytes, UTF-16LE, zero-padded | — |
| 9 | logfont: LF_FACESIZE is enforced, not truncated into | — |
| 10 | logfont: non-ASCII is refused, because the blob is built bytewise | — |
| 11 | logfont: an empty family is all padding, never a malformed blob | — |
| 12 | cpu topology: caps at 8 on a machine with more | — |
| 13 | cpu topology: an affinity-constrained run reports the allowed count | — |
| 14 | cpu topology: a contiguous affinity range counts inclusively | — |
| 15 | cpu topology: says nothing when unconstrained and at or below the cap | — |
| 16 | cpu topology: a constrained set below the cap wins over the cap | — |
| 17 | windowmetrics: a value wrapped across continuation lines is rejoined | scripts/ableton-live — sync_metric_fonts splices over bytes 28..91; a truncated blob corrupts DPI heights |
| 18 | windowmetrics: an unwrapped value reads back verbatim | — |
| 19 | windowmetrics: a value in another section is not picked up | — |
| 20 | windowmetrics: a missing user.reg is silent, not an error cascade | — |

<a id="patch-stack"></a>

## tests/patch-stack.bats


The patch stack, verified without building Wine.

Today a patch that no longer applies is caught only by the full container
build in ci-pr-build.yml — tens of minutes, and only on PRs that touch the
paths in that workflow's filter. Everything here reproduces the apply half of
scripts/container-build.sh against the vendored base and finishes in seconds,
so a bad rebase fails in the first minute of CI instead of the fortieth.

This does NOT replace the build: it cannot catch a patch that applies but
fails to compile. It catches the far more common case — a patch that has
rotted against the base, was renumbered, or was added without being frozen.

| # | Test | Guards |
| --- | --- | --- |
| 1 | series: every frozen patch still hashes to its recorded sha256 | — |
| 2 | series: no patch on disk is missing from the freeze manifest | — |
| 3 | series: no frozen patch has vanished from disk | — |
| 4 | wine series applies cleanly to the pinned base, in order | — |
| 5 | pipeasio series applies cleanly to the pinned pipeasio tarball | — |
| 6 | no patch needs the 3-way fallback — context drift is worth acting on | scripts/container-build.sh — git am --3way silently rescues drifted context, and that rescue is what breaks at the next base bump |
| 7 | audit: every wine patch is registered in FINGERPRINTS or STAMP_ONLY | scripts/build-audit.sh — an unregistered patch is one nobody verifies in the shipped artifact |
| 8 | audit: no FINGERPRINTS or STAMP_ONLY entry refers to a deleted patch | — |
| 9 | numbering: every gap in the series is documented in SERIES_GAPS | — |
| 10 | numbering: no two patches share a number | — |
| 11 | patches: every wine patch carries a Subject and a diff | — |
| 12 | patches: pipeasio patches are plain diffs, as patch -p1 expects | — |

## Cross-reference

Issues, commits and source sites cited by a `# guards:` annotation.

| Reference | Tests |
| --- | --- |
| `.bats-core is a full clone of another project; run.sh's comment said it` | repo-hygiene: the vendored bats clone is ignored |
| `a bats on PATH used to beat the pin, so a checkout ran whatever the` | repo-hygiene: CI runs the bats tests/run.sh pins, not one of its own |
| `commit 9cba3b0` | launcher: gray text: the dark fallback lands on classic GrayText |
| `commit f0fc05e` | detect-scale: cosmic probe: a disabled lid never wins when it is marked non-primary<br>detect-scale: cosmic probe: a disabled lid never wins, even with no primary line |
| `commit f84eaa4` | repo-hygiene: runtime name: every live file agrees on one wine-d2d1-nspa version |
| `issue #106` | repo-hygiene: desktop entries validate after substitution |
| `issue #32` | launcher: gray text: the blend is 45% towards MenuText, per channel, not symmetric |
| `issue #38` | launcher-cli: a .als set goes straight to the Live exe, never through start.exe<br>launcher-cli: clips and packs route the same way as sets, case-insensitively |
| `issue label 'installer'` | packaging: every script a kit script sources is itself staged into the kit |
| `licence GPLv2+` | packaging: the kit ships the GPL source and licence Ableton Link requires |
| `lifting runtime_pids into the lib renamed it, and a replace that only` | packaging: every shell function a script calls is actually defined |
| `scripts/ableton-live` | launcher-cli: a stale wineserver is killed and the session booted before registry writes<br>launcher: windowmetrics: a value wrapped across continuation lines is rejoined |
| `scripts/build-audit.sh` | patch-stack: audit: every wine patch is registered in FINGERPRINTS or STAMP_ONLY |
| `scripts/container-build.sh` | patch-stack: no patch needs the 3-way fallback — context drift is worth acting on |
| `scripts/detect-scale.sh DPI policy` | detect-scale: block map: gnome scales collapse onto the ceil-based matched set<br>detect-scale: block map: non-gnome scales round to plain LogPixels with no IFEO |
| `scripts/detect-theme.sh` | detect-theme: newest prefs dir: mtime wins, not a version sort<br>detect-theme: newest prefs dir: the sort -V trap case, stated explicitly |
| `scripts/setup-run-header.sh line 19` | repo-hygiene: the installer header survives being run by a real POSIX sh |
| `the container sees only what build.sh passes with -e, and an unset` | repo-hygiene: build.sh forwards every variable container-build.sh reads from its environment |
| `the staging list is recovered by anchored sed, so a reformat of` | packaging: the kit staging list is still parseable out of make-installer.sh |
